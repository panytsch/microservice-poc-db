base path /v1/rest

 method 	create
			get user => {
							name
							password
						}

{
	id: 0
 	name: ''
}

method make transaction => user_token + amount
		get one => token + id
		get list => token + limit
//todo add amount

mock amount
{
	id: 0
	status: 0
}
// todo make table
method request refund => user_token + amount
		change status of refund => token + id
		get one => token + id
		get list => token + limit
{
	refundID
	status
}
